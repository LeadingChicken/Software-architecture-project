import RootLayout from "./layout";
export const layout = RootLayout;
import React from "react";

const Home = () => (
  <div>
    <h1>Welcome to VOU</h1>
    <p>Choose an option from the menu to get started.</p>
  </div>
);

export default Home;
